# UPVS API для ChatGPT Actions

API для доступа к справочнику UPVS (Укрупненные Показатели Восстановительной Стоимости) через ChatGPT Actions.

## 🚀 Быстрый старт

### 1. Настройка API ключа

Скопируйте файл `.env.example` в `.env` и установите свой API ключ:

```bash
cp .env.example .env
nano .env  # или используйте любой редактор
```

Установите свой уникальный API ключ в `.env`:
```
API_KEY=ваш-секретный-ключ-здесь
```

### 2. Запуск через Docker Compose

Запустите все сервисы одной командой:

```bash
docker-compose up -d
```

Эта команда:
- Поднимет PostgreSQL базу данных
- Загрузит данные из CSV/JSONL файлов (init контейнер)
- Запустит API сервер на порту 8000

### 3. Проверка работоспособности

Откройте в браузере:
- **Swagger UI**: http://localhost:8000/docs
- **Health check**: http://localhost:8000/health
- **OpenAPI schema**: http://localhost:8000/.well-known/openapi.json

## 📚 Структура API

### Основные эндпоинты

#### 🌲 Навигация по дереву страниц

- `GET /api/tree` - Полное дерево всех страниц справочника
- `GET /api/tree/search?query=...` - Поиск страниц по названию

#### 📄 Получение содержимого страниц

- `GET /api/page/{page_id}` - Вся информация о странице (текст + таблицы)

#### 📊 Работа с таблицами

- `GET /api/tables/list` - Список всех таблиц с названиями
- `GET /api/tables/search?query=...` - Поиск таблиц по названию
- `GET /api/table/{table_id}` - Полные данные конкретной таблицы
- `GET /api/page/{page_id}/tables` - Все таблицы конкретной страницы

## 🔐 Аутентификация

Все эндпоинты (кроме `/health` и `/docs`) требуют Bearer токен аутентификации.

### Пример запроса с curl:

```bash
curl -H "Authorization: Bearer ваш-секретный-ключ-здесь" \
     http://localhost:8000/api/tree
```

### Пример с Python:

```python
import requests

headers = {
    "Authorization": "Bearer ваш-секретный-ключ-здесь"
}

response = requests.get("http://localhost:8000/api/tree", headers=headers)
data = response.json()
```

## 🤖 Настройка ChatGPT Action

### 1. Получите OpenAPI schema

Скачайте файл schema:
```bash
curl http://localhost:8000/.well-known/openapi.json > upvs-openapi.json
```

Или откройте в браузере: http://localhost:8000/.well-known/openapi.json

### 2. Создайте Action в ChatGPT

1. Откройте ChatGPT → Настройки → Actions
2. Создайте новый Action
3. Вставьте содержимое OpenAPI schema
4. Настройте аутентификацию:
   - **Auth Type**: Bearer
   - **Token**: ваш API ключ из `.env`

### 3. Настройте базовый URL

Если ваш API доступен публично (например, через ngrok или на сервере):
- Замените `http://localhost:8000` на ваш публичный URL
- Убедитесь, что используется HTTPS

### 4. Пример промпта для ChatGPT

После настройки Action, вы можете спросить у ChatGPT:

```
Найди в справочнике UPVS информацию о удельных весах для жилых зданий
```

ChatGPT автоматически:
1. Получит дерево страниц через `/api/tree`
2. Найдет нужный раздел
3. Запросит содержимое страницы через `/api/page/{page_id}`
4. Или найдет таблицу через `/api/tables/search`
5. Получит данные таблицы через `/api/table/{table_id}`

## 🛠️ Управление сервисами

### Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Только API
docker-compose logs -f api

# Только база данных
docker-compose logs -f postgres
```

### Остановка сервисов

```bash
docker-compose down
```

### Остановка с удалением данных

```bash
docker-compose down -v
```

### Перезапуск API

```bash
docker-compose restart api
```

### Пересборка контейнеров

```bash
docker-compose up -d --build
```

## 📊 Структура данных

API работает с данными из папки `data/`:

```
data/
├── raw/
│   ├── pages.csv          # Информация о страницах
│   ├── text_chunks.jsonl  # Текстовые блоки
│   ├── tables.jsonl       # Таблицы с данными
│   └── edges.csv          # Связи между страницами
└── derived/
    └── ...                # Производные данные (не используются в новой версии)
```

## 🔧 Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `DATABASE_URL` | URL подключения к PostgreSQL | `postgresql://upvs:upvs@postgres:5432/upvs` |
| `API_KEY` | Секретный ключ для Bearer аутентификации | `your-secret-api-key-here` |

## 🐛 Решение проблем

### API не запускается

Проверьте логи:
```bash
docker-compose logs api
```

### База данных не подключается

Убедитесь, что PostgreSQL запущен:
```bash
docker-compose ps postgres
docker-compose logs postgres
```

### Ошибка 401 Unauthorized

Проверьте, что вы передаете правильный API ключ в заголовке `Authorization: Bearer <ваш-ключ>`

### Нет данных в API

Проверьте, что init контейнер успешно загрузил данные:
```bash
docker-compose logs init
```

Если нужно переинициализировать данные:
```bash
docker-compose down -v
docker-compose up -d
```

## 📝 Примеры использования

### Получить дерево разделов

```bash
curl -H "Authorization: Bearer ваш-ключ" \
     http://localhost:8000/api/tree
```

### Найти страницу про жилые здания

```bash
curl -H "Authorization: Bearer ваш-ключ" \
     "http://localhost:8000/api/tree/search?query=жилые%20здания"
```

### Получить содержимое страницы

```bash
curl -H "Authorization: Bearer ваш-ключ" \
     http://localhost:8000/api/page/PAGE_ID
```

### Найти таблицу по названию

```bash
curl -H "Authorization: Bearer ваш-ключ" \
     "http://localhost:8000/api/tables/search?query=удельные%20веса"
```

### Получить данные таблицы

```bash
curl -H "Authorization: Bearer ваш-ключ" \
     http://localhost:8000/api/table/TABLE_ID
```

## 🌐 Публичный доступ (для ChatGPT)

### Вариант 1: ngrok (для тестирования)

```bash
# Установите ngrok: https://ngrok.com/

# Запустите туннель
ngrok http 8000
```

Используйте предоставленный ngrok URL (например, `https://abc123.ngrok.io`) в качестве базового URL для ChatGPT Action.

### Вариант 2: Деплой на сервер

Разверните приложение на сервере с публичным IP и настройте:
- HTTPS (обязательно для ChatGPT Actions)
- Nginx как reverse proxy
- SSL сертификат (Let's Encrypt)

## 📄 Лицензия

Этот проект создан для работы со справочником UPVS.

