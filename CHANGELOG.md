# Changelog

## Версия 2.0.0 - Переход на ChatGPT Actions (2026-01-09)

### 🎯 Главные изменения

Проект полностью переориентирован на использование как бекенда для ChatGPT Actions вместо встроенного RAG.

### ✅ Добавлено

- **Bearer Token аутентификация** через API ключ
- **Новые эндпоинты для ChatGPT**:
  - `GET /api/tree` - полное дерево страниц
  - `GET /api/tree/search` - поиск страниц
  - `GET /api/page/{page_id}` - полная информация о странице
  - `GET /api/tables/list` - список всех таблиц
  - `GET /api/tables/search` - поиск таблиц
  - `GET /api/table/{table_id}` - данные таблицы
  - `GET /api/page/{page_id}/tables` - таблицы страницы
- **OpenAPI schema endpoint**: `/openapi.json` для импорта в ChatGPT
- **Улучшенная документация**:
  - `README.md` - главная документация
  - `QUICKSTART.md` - быстрый старт
  - `README_CHATGPT.md` - подробное руководство
  - `CHATGPT_SETUP.md` - настройка ChatGPT Actions
  - `ENV_EXAMPLE.txt` - пример переменных окружения
- **Упрощенный docker-compose** - только PostgreSQL и API

### ❌ Удалено

- **vLLM сервис** и поддержка локальных LLM
- **FAISS индексы** и векторный поиск
- **Embeddings модули** (sentence-transformers)
- **Web фронтенд** (Next.js)
- **RAG эндпоинты**: `/rag`, `/context`, `/search`
- Зависимости:
  - `faiss-cpu`
  - `sentence-transformers`
  - `numpy`
  - `requests` (для vLLM)

### 🔄 Изменено

- **API структура**: переход с RAG эндпоинтов на data-first подход
- **Аутентификация**: с отсутствия на Bearer token
- **Документация**: полностью переписана для ChatGPT Actions
- **Config**: убраны параметры embeddings и vLLM
- **init_data.py**: убрана сборка FAISS индекса
- **requirements.txt**: минимальный набор зависимостей

### 🏗️ Архитектура

#### До (v1.x):
```
User → Web UI → FastAPI API → FAISS Search → vLLM → Response
                           ↓
                      PostgreSQL
```

#### После (v2.0):
```
ChatGPT Actions → FastAPI API → PostgreSQL → Structured Data
                      ↓
                Bearer Auth
```

### 📝 Миграция с v1.x

1. Остановите старые контейнеры:
   ```bash
   docker-compose down -v
   ```

2. Обновите код до v2.0

3. Создайте `.env` файл с API ключом:
   ```bash
   echo "API_KEY=your-secret-key" > .env
   ```

4. Запустите новую версию:
   ```bash
   docker-compose up -d
   ```

5. Настройте ChatGPT Action (см. `CHATGPT_SETUP.md`)

### 🔧 Технические детали

**Было:**
- FastAPI с FAISS векторным поиском
- vLLM для генерации ответов
- sentence-transformers для эмбеддингов
- Web фронтенд на Next.js
- RAG pipeline с контекстным поиском

**Стало:**
- Чистый REST API с PostgreSQL
- Структурированные данные (дерево, страницы, таблицы)
- Bearer token аутентификация
- OpenAPI schema для ChatGPT
- ChatGPT делает анализ и генерацию ответов

### 💡 Почему эти изменения?

1. **ChatGPT умеет лучше** - современные LLM эффективнее анализируют структурированные данные
2. **Проще поддержка** - не нужно управлять локальными моделями и GPU
3. **Быстрее запуск** - docker-compose up за ~30 секунд вместо 5+ минут
4. **Меньше зависимостей** - 4 пакета вместо 8+
5. **Гибче интеграция** - API можно использовать из любых приложений

### 🎯 Целевой Use Case

**v1.x**: Самостоятельное веб-приложение с RAG

**v2.0**: Бекенд для ChatGPT Actions (но можно использовать из любого клиента)

### 📊 Статистика изменений

- Удалено файлов: 3 (embeddings.py, faiss_store.py, весь web/)
- Изменено файлов: 4 (main.py, config.py, requirements.txt, docker-compose.yml)
- Добавлено документации: 4 файла
- Строк кода API: ~400 (было ~700)
- Зависимостей: 4 (было 8)
- Сервисов Docker: 3 (было 5)

### 🚀 Следующие шаги

1. Запустите проект локально
2. Протестируйте эндпоинты через Swagger UI
3. Настройте публичный доступ (ngrok или сервер)
4. Создайте ChatGPT Action
5. Протестируйте интеграцию

---

## Версия 1.x (legacy)

### Возможности v1.x

- Встроенный RAG с vLLM
- FAISS векторный поиск
- Web интерфейс на Next.js
- Локальные LLM модели

**Примечание**: v1.x больше не поддерживается. Для использования старой версии откатитесь на тег `v1.x` в Git.

