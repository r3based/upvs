# 🤖 Настройка ChatGPT Action для UPVS API

Пошаговая инструкция по настройке ChatGPT Action для работы со справочником UPVS.

## Предварительные требования

1. ✅ API запущен и доступен по публичному URL (с HTTPS!)
2. ✅ У вас есть API ключ из `.env` файла
3. ✅ У вас есть доступ к ChatGPT Plus или Enterprise с возможностью создавать Actions

## Шаг 1: Подготовка API для публичного доступа

### Вариант А: Локальная разработка с ngrok

```bash
# Установите ngrok: https://ngrok.com/download

# Запустите туннель
ngrok http 8000
```

Вы получите URL вида: `https://abc123.ngrok.io`

**Важно:** 
- ngrok URL работает только пока запущен ngrok
- Бесплатная версия дает случайный URL при каждом запуске
- Для постоянного URL нужна платная подписка ngrok

### Вариант Б: Деплой на сервер

Разверните на VPS/облаке с:
- Публичным IP
- HTTPS сертификатом (Let's Encrypt)
- Nginx как reverse proxy

Пример конфига Nginx:

```nginx
server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl;
    server_name your-domain.com;
    
    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
    
    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## Шаг 2: Получение OpenAPI Schema

### Способ 1: Прямое скачивание

```bash
curl https://your-domain.com/openapi.json > upvs-openapi.json
```

### Способ 2: Из браузера

Откройте: `https://your-domain.com/openapi.json`

Сохраните содержимое в файл.

### Важные правки в schema

Откройте `upvs-openapi.json` и измените `servers`:

```json
{
  "openapi": "3.1.0",
  "info": { ... },
  "servers": [
    {
      "url": "https://your-actual-domain.com",
      "description": "Production server"
    }
  ],
  ...
}
```

## Шаг 3: Создание Action в ChatGPT

1. Откройте ChatGPT
2. Перейдите в **Configure** (иконка настроек внизу слева)
3. Выберите **Actions** → **Create a GPT**
4. В разделе **Actions** нажмите **Create new action**

## Шаг 4: Импорт Schema

### Вариант 1: Импорт из URL

```
Schema: Import from URL
URL: https://your-domain.com/openapi.json
```

### Вариант 2: Вставка напрямую

Скопируйте содержимое файла `upvs-openapi.json` и вставьте в поле **Schema**.

## Шаг 5: Настройка аутентификации

1. В разделе **Authentication** выберите:
   - **Authentication Type**: `Bearer`
   
2. Введите ваш API ключ:
   - **Bearer Token**: `your-secret-key-here` (из `.env` файла)

3. Сохраните настройки

## Шаг 6: Настройка GPT

### Instructions (Инструкции для GPT)

Добавьте следующие инструкции:

```
Ты помощник по справочнику UPVS (Укрупненные Показатели Восстановительной Стоимости).

ВАЖНЫЕ ПРАВИЛА:
1. Всегда используй API для получения актуальных данных
2. Никогда не придумывай данные - используй только то, что получил из API
3. Если не нашел информацию - так и скажи пользователю

РЕКОМЕНДУЕМЫЙ WORKFLOW:
1. Для общих вопросов: сначала получи дерево через /api/tree
2. Для поиска конкретной информации: используй /api/tree/search
3. Для получения содержимого: используй /api/page/{page_id}
4. Для поиска таблицы: используй /api/tables/search
5. Для получения данных таблицы: используй /api/table/{table_id}

ФОРМАТ ОТВЕТОВ:
- Всегда указывай источник (название страницы, таблицы)
- Если данные в таблице - форматируй их читабельно
- Предоставляй конкретные числа, не округляй без необходимости
- Ссылайся на номера таблиц из справочника

ПРИМЕРЫ ЗАПРОСОВ:
- "Найди удельный вес для жилых зданий"
  → Используй /api/tables/search?query=удельный+вес+жилые
  → Получи table_id
  → Запроси /api/table/{table_id}
  → Представь данные пользователю

- "Что говорится в разделе про кирпичные здания?"
  → Используй /api/tree/search?query=кирпичные
  → Получи page_id
  → Запроси /api/page/{page_id}
  → Представь информацию пользователю
```

### Conversation starters (Примеры вопросов)

Добавьте примеры вопросов:

```
- Найди удельный вес для жилых зданий
- Покажи таблицу коэффициентов для деревянных конструкций
- Какие разделы есть в справочнике?
- Что говорится про кирпичные здания?
```

### Name & Description

```
Name: UPVS Справочник
Description: Помощник по справочнику UPVS для поиска строительных нормативов и коэффициентов
```

## Шаг 7: Тестирование

### Тест 1: Получение дерева

Спросите:
```
Какие разделы есть в справочнике?
```

GPT должен вызвать `/api/tree` и показать структуру.

### Тест 2: Поиск таблицы

Спросите:
```
Найди таблицу с удельными весами для жилых зданий
```

GPT должен:
1. Вызвать `/api/tables/search?query=удельные+веса+жилые`
2. Получить список таблиц
3. Вызвать `/api/table/{table_id}` для нужной таблицы
4. Показать данные

### Тест 3: Получение страницы

Спросите:
```
Расскажи про кирпичные здания
```

GPT должен:
1. Вызвать `/api/tree/search?query=кирпичные`
2. Получить page_id
3. Вызвать `/api/page/{page_id}`
4. Показать содержимое

## Шаг 8: Публикация (опционально)

Если хотите поделиться GPT:

1. Нажмите **Save** в правом верхнем углу
2. Выберите:
   - **Only me** - только для вас
   - **Anyone with a link** - для всех, у кого есть ссылка
   - **Public** - публичный доступ (требуется модерация)

**Важно:** Убедитесь, что ваш API будет доступен для всех пользователей GPT!

## 🔒 Безопасность

### Рекомендации

1. **Используйте сильный API ключ**
   ```bash
   # Генерация случайного ключа
   openssl rand -base64 32
   ```

2. **Ограничьте rate limiting** (опционально)
   Добавьте middleware в FastAPI для ограничения запросов

3. **Мониторьте логи**
   ```bash
   docker-compose logs -f api
   ```

4. **Используйте HTTPS** (обязательно для ChatGPT!)

5. **Регулярно меняйте API ключ**

## 🐛 Решение проблем

### "Authentication failed"

Проверьте:
- Правильно ли указан Bearer token
- Нет ли лишних пробелов в токене
- Совпадает ли токен с `API_KEY` в `.env`

### "Connection timeout"

Проверьте:
- Доступен ли API по URL (откройте в браузере)
- Работает ли HTTPS (ChatGPT требует HTTPS!)
- Нет ли блокировки файрволом

### "Invalid schema"

Проверьте:
- Корректность JSON в OpenAPI schema
- Все ли серверы в `servers` доступны
- Правильно ли указаны типы параметров

### GPT не вызывает API

Проверьте:
- Добавлены ли инструкции в **Instructions**
- Правильно ли настроена аутентификация
- Сохранили ли вы GPT после настройки

## 📝 Примеры запросов

### Curl тестирование эндпоинтов

```bash
# Проверка здоровья (без auth)
curl https://your-domain.com/health

# Получение дерева
curl -H "Authorization: Bearer YOUR_KEY" \
     https://your-domain.com/api/tree

# Поиск таблицы
curl -H "Authorization: Bearer YOUR_KEY" \
     "https://your-domain.com/api/tables/search?query=жилые"

# Получение таблицы
curl -H "Authorization: Bearer YOUR_KEY" \
     https://your-domain.com/api/table/TABLE_ID
```

## 🎉 Готово!

Теперь вы можете спрашивать ChatGPT о данных из справочника UPVS, и он будет автоматически использовать ваш API для получения актуальной информации.

## 📚 Дополнительные ресурсы

- [OpenAI Actions Documentation](https://platform.openai.com/docs/actions)
- [FastAPI Security](https://fastapi.tiangolo.com/tutorial/security/)
- [ngrok Documentation](https://ngrok.com/docs)

