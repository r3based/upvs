# UPVS API для ChatGPT Actions

REST API для доступа к справочнику **UPVS** (Укрупненные Показатели Восстановительной Стоимости) через ChatGPT Actions.

## 🎯 Описание проекта

Это бекенд-сервис, который предоставляет структурированный доступ к данным справочника UPVS через REST API. 

**Основные возможности:**
- 🔍 Получение полного дерева разделов справочника
- 📄 Доступ к содержимому страниц (текст + таблицы)
- 📊 Поиск и получение данных таблиц по названию
- 🔐 Защита через Bearer token аутентификацию
- 📚 Swagger UI документация
- 🤖 Готов к интеграции с ChatGPT Actions

## 🏗️ Архитектура

```
┌─────────────────┐
│   ChatGPT       │
│   Actions       │
└────────┬────────┘
         │ REST API (Bearer Auth)
         ▼
┌─────────────────┐
│   FastAPI       │
│   (Python)      │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   PostgreSQL    │
│   (База данных) │
└─────────────────┘
```

**Что убрали из старой версии:**
- ❌ vLLM и локальные LLM модели
- ❌ FAISS и векторный поиск
- ❌ Встроенный RAG модуль
- ❌ Web фронтенд

**Зачем:** ChatGPT сам умеет анализировать данные и выбирать нужные таблицы на основе API ответов.

## 🚀 Быстрый старт

### Требования

- Docker и Docker Compose
- Данные в формате CSV/JSONL в папке `data/raw/`:
  - `pages.csv`
  - `text_chunks.jsonl`
  - `tables.jsonl`
  - `edges.csv`

### Шаг 1: Настройка API ключа

Создайте файл `.env`:

```bash
echo "API_KEY=your-secret-key-here" > .env
```

> ⚠️ Используйте надежный случайный ключ в продакшене!

### Шаг 2: Запуск

```bash
docker-compose up -d
```

API будет доступен на `http://localhost:8000`

### Шаг 3: Проверка

Откройте в браузере:

- **Swagger UI**: http://localhost:8000/docs
- **Health check**: http://localhost:8000/health
- **OpenAPI schema**: http://localhost:8000/openapi.json

## 📖 Документация

- [QUICKSTART.md](./QUICKSTART.md) - краткая инструкция по запуску
- [README_CHATGPT.md](./README_CHATGPT.md) - подробная документация и настройка ChatGPT Actions

## 🔌 API Эндпоинты

### Навигация

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/tree` | Полное дерево всех страниц |
| GET | `/api/tree/search?query=...` | Поиск страниц по названию |

### Контент страниц

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/page/{page_id}` | Вся информация о странице (текст + таблицы) |

### Таблицы

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/api/tables/list` | Список всех таблиц с названиями |
| GET | `/api/tables/search?query=...` | Поиск таблиц по названию |
| GET | `/api/table/{table_id}` | Данные конкретной таблицы |
| GET | `/api/page/{page_id}/tables` | Все таблицы страницы |

### Системные

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/health` | Проверка состояния (без auth) |
| GET | `/docs` | Swagger UI (без auth) |
| GET | `/openapi.json` | OpenAPI schema для импорта |

## 🔐 Аутентификация

Все эндпоинты (кроме `/health` и `/docs`) требуют Bearer token:

```bash
curl -H "Authorization: Bearer your-secret-key-here" \
     http://localhost:8000/api/tree
```

## 🤖 Настройка ChatGPT Actions

### 1. Получите OpenAPI schema

```bash
curl http://localhost:8000/openapi.json > upvs-openapi.json
```

### 2. Настройте публичный доступ

**Для разработки (ngrok):**
```bash
ngrok http 8000
```

**Для продакшена:**
- Разверните на сервере с публичным IP
- Настройте HTTPS (обязательно для ChatGPT!)
- Используйте Nginx как reverse proxy

### 3. Создайте Action в ChatGPT

1. Откройте ChatGPT → Configure → Actions
2. Создайте новый Action
3. Импортируйте OpenAPI schema
4. Настройте аутентификацию:
   - **Auth Type**: Bearer
   - **Bearer Token**: ваш API ключ

### 4. Тестируйте!

Спросите у ChatGPT:
```
Найди в справочнике UPVS информацию о удельных весах для жилых зданий
```

ChatGPT автоматически:
1. Получит дерево разделов
2. Найдет нужную страницу
3. Запросит таблицу
4. Предоставит ответ на основе данных

## 📁 Структура проекта

```
.
├── apps/
│   └── api/              # FastAPI приложение
│       ├── main.py       # Основной файл с эндпоинтами
│       ├── config.py     # Конфигурация
│       ├── db.py         # Работа с PostgreSQL
│       ├── Dockerfile
│       └── requirements.txt
├── scripts/
│   ├── init_data.py      # Скрипт инициализации данных
│   └── load_postgres/    # Загрузка данных в БД
│       └── load_all.py
├── data/
│   └── raw/              # Исходные данные
│       ├── pages.csv
│       ├── text_chunks.jsonl
│       ├── tables.jsonl
│       └── edges.csv
├── docker-compose.yml    # Конфигурация Docker
├── README.md            # Этот файл
├── README_CHATGPT.md    # Подробная документация
├── QUICKSTART.md        # Быстрый старт
└── ENV_EXAMPLE.txt      # Пример .env файла
```

## 🛠️ Команды управления

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Перезапуск API
docker-compose restart api

# Просмотр логов
docker-compose logs -f api

# Пересборка и запуск
docker-compose up -d --build

# Остановка с удалением данных
docker-compose down -v
```

## 🔧 Переменные окружения

Создайте файл `.env` на основе `ENV_EXAMPLE.txt`:

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `DATABASE_URL` | URL PostgreSQL | `postgresql://upvs:upvs@postgres:5432/upvs` |
| `API_KEY` | Bearer token для API | `your-secret-api-key-here` |

## 📊 Формат данных

### pages.csv

```csv
page_id,url,title,parent_url,breadcrumbs,toc,fetched_at,http_status,content_hash
```

### text_chunks.jsonl

```json
{"chunk_id": "...", "page_id": "...", "chunk_index": 0, "source_order": 1, "section_path": [...], "text": "..."}
```

### tables.jsonl

```json
{"table_id": "...", "page_id": "...", "table_index": 0, "source_order": 2, "section_path": [...], "caption": "...", "columns": [...], "rows": [[...]]}
```

## 🐛 Решение проблем

### API не запускается

```bash
# Проверьте логи
docker-compose logs api

# Проверьте, что PostgreSQL запущен
docker-compose ps
```

### База данных пустая

```bash
# Проверьте логи init контейнера
docker-compose logs init

# Убедитесь, что файлы есть в data/raw/
ls -la data/raw/

# Переинициализируйте
docker-compose down -v
docker-compose up -d
```

### 401 Unauthorized

Проверьте:
- Правильный ли API ключ в `.env`
- Передаете ли вы заголовок `Authorization: Bearer YOUR_KEY`

### Нет данных в эндпоинтах

Проверьте, что данные загружены в PostgreSQL:

```bash
docker-compose exec postgres psql -U upvs -d upvs -c "SELECT COUNT(*) FROM pages;"
docker-compose exec postgres psql -U upvs -d upvs -c "SELECT COUNT(*) FROM tables;"
```

## 📝 Пример использования с Python

```python
import requests

API_BASE = "http://localhost:8000"
API_KEY = "your-secret-key-here"

headers = {
    "Authorization": f"Bearer {API_KEY}"
}

# Получить дерево
response = requests.get(f"{API_BASE}/api/tree", headers=headers)
tree = response.json()

# Найти таблицу
response = requests.get(
    f"{API_BASE}/api/tables/search",
    params={"query": "жилые здания"},
    headers=headers
)
tables = response.json()

# Получить данные таблицы
table_id = tables["tables"][0]["table_id"]
response = requests.get(f"{API_BASE}/api/table/{table_id}", headers=headers)
table_data = response.json()

print(f"Таблица: {table_data['caption']}")
print(f"Колонки: {table_data['columns']}")
print(f"Строк: {len(table_data['rows'])}")
```

## 🤝 Вклад в проект

Этот проект создан для работы со справочником UPVS и интеграции с ChatGPT Actions.

## 🔒 Безопасность

Проект включает **enterprise-grade** защиту:
- 🔥 Firewall (UFW)
- 🛡️ IDS (Fail2Ban)
- 🔐 SSH hardening
- 🌐 Nginx rate limiting
- 🔑 Bearer Token auth
- 📊 Мониторинг

**Полное руководство:** [SECURITY.md](./SECURITY.md)

**Быстрая настройка:**
```bash
sudo bash scripts/security/setup_all.sh
```

## 📄 Лицензия

См. LICENSE файл для деталей.

## 🔗 Полезные ссылки

- [FastAPI документация](https://fastapi.tiangolo.com/)
- [OpenAPI спецификация](https://swagger.io/specification/)
- [ChatGPT Actions](https://platform.openai.com/docs/actions)
- [PostgreSQL](https://www.postgresql.org/)
- [OWASP Security](https://owasp.org/)
